[options]
work_mode = "sys"
library = "GdkX11"
version = "3.0"
min_cfg_version = "3.22"
single_version_file = "."
girs_directories = ["../../gir-files"]

external_libraries = [
    "Cairo",
    "Gdk",
    "GLib",
]

ignore = [
]

[[feature_dependencies]]
version = "3.24.2"
dependencies = [
    "cairo"
]
