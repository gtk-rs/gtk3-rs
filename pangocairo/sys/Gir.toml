[options]
work_mode = "sys"
library = "PangoCairo"
version = "1.0"
min_cfg_version = "1.38"
single_version_file = "."
girs_directories = ["../../gir-files"]

external_libraries = [
    "GLib",
    "Pango",
    "Cairo",
]
